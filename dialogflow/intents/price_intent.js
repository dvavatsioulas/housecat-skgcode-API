exports.price = function (agent){

    const price = agent.parameters['price'];
    //const gotPrice = price.length > 0;
    
    /*
    if(gotPrice) {
        agent.add(`Nice, the price should be ${price}`);
        console.log(JSON.stringify(price))

        userPrice = price

        module.exports.userPrice = userPrice;
    } 
    */
}